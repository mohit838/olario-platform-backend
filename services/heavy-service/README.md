# heavy-service (C++)

## Build
```bash
cd services/heavy-service
cmake -S . -B build
cmake --build build
```

## Run
```bash
./build/heavy-service
```

This is a minimal bootstrap binary. Drogon integration can be added next.
